"""Tests package for the self-hosted demo `sample` app.

Intentionally a near-empty placeholder. The `tests/unit/` subdirectory is the
target the `c2_write_tests` benchmark task asks Claude Code to populate
(writing pytest cases for `sample/utils/path_utils.py`); see
[../../BENCHMARK.md](../../BENCHMARK.md) for details.

Existing tests are not committed here so each benchmark run starts from a
clean slate.
"""
